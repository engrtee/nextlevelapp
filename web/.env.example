VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Single-user PIN gate: create exactly one Supabase Auth user (Authentication ->
# Users -> Add user) with this email and a PIN/password of your choice. The
# login screen only asks for the PIN; it signs in as this fixed account.
VITE_OWNER_EMAIL=owner@job-assistant.local
